# PitPanda Event Listener

To setup make a servers folder in the `src` directory and copy what is seen in the `example_servers` directory.

then these commands

`npm install`

`npm run build`

`npm start`

note everytime you change your `servers` folder you will need to rebuild
